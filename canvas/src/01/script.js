;(function(win, doc) {

    'use strict';

    win.onload = function() {
        var canvas = doc.getElementById('canvas'),
            ctx = canvas.getContext('2d');

    };

})(this, this.document);
